vim.g.vim_ai_chat = {
  options = {
      model = "gpt-4",
      temperature = 0.2,
  }
}


-- vim.cmd('vnew | r ! git diff --staged')
-- %AIE Write git commit message ready to copy-paste in GIT-EDITMSG. Output format is a title (one), a blank line (one) and then bullet points
